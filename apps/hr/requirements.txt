# Core
Django==5.1.4
psycopg[binary]==3.1.19
redis==5.2.1

# Async task queue
celery==5.4.0
django-celery-beat==2.7.0
django-celery-results==2.5.1

# Template & frontend helpers
django-htmx==1.21.0
whitenoise==6.8.2

# Storage
django-storages==1.14.4
boto3==1.35.90            # for DigitalOcean Spaces (S3-compatible)

# Email
django-anymail==12.0

# Template engine (HR letter generation)
Jinja2>=3.1.0

# PDF generation (WeasyPrint on Linux prod; xhtml2pdf fallback for Windows dev)
weasyprint==62.3
pydyf==0.11.0  # 0.12+ breaks WeasyPrint 62.x (transform API removed)
xhtml2pdf==0.2.16

# Excel
openpyxl==3.1.5

# Encryption (Aadhaar, account numbers)
cryptography>=44.0.0

# MFA (TOTP)
pyotp==2.9.0
qrcode[pil]==8.0

# Stronger password hashing (activated via PASSWORD_HASHERS once installed)
argon2-cffi==23.1.0

# Environment management
python-decouple==3.8

# Request context (for TenantManager)
django-crum==0.7.9

# Audit trail
django-simple-history==3.7.0

# CORS (for future mobile API)
django-cors-headers==4.6.0

# Image handling (profile photos)
Pillow==11.1.0

# Utilities
python-dateutil==2.9.0
phonenumbers==8.13.52
requests==2.32.3

# Development
django-debug-toolbar==4.4.6
ipython==8.30.0

# Production
gunicorn==23.0.0
sentry-sdk[django]==2.19.2
anthropic>=0.100.0

# Resume parsing (recruitment AI â€” PDF/DOCX text extraction)
pdfplumber==0.11.4
pypdf==5.1.0
python-docx==1.1.2

# Local embeddings for hybrid resume matching (ONNX runtime, no torch)
# 0.8.0 is the first line compatible with Pillow 11 (0.4.x pins pillow<11).
fastembed==0.8.0
numpy>=1.26,<2.2
